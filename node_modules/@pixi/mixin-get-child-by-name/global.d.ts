declare namespace GlobalMixins
{
    interface DisplayObject
    {
        name: string | null;
    }

    interface Container
    {
        getChildByName<T extends import('../display/lib').DisplayObject = import('../display/lib').DisplayObject>(
            name: string,
            deep?: boolean,
        ): T | null;
    }
}
