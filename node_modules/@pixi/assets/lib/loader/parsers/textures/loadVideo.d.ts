import type { IBaseTextureOptions, IVideoResourceOptions, Texture } from '../../../../../core/lib';
import type { LoaderParser } from '../LoaderParser';
/**
 * Configuration for the `loadVideo` loader paarser.
 * @memberof PIXI
 * @see PIXI.loadVideo
 */
export interface LoadVideoConfig {
    /**
     * When set to `true`, the video will start playing automatically after being loaded,
     * otherwise it will not start playing automatically.
     * @default true
     */
    defaultAutoPlay: boolean;
}
/**
 * Loads videos into Textures.
 * @memberof PIXI
 */
export declare const loadVideo: LoaderParser<Texture<import("../../../../../core/lib").Resource>, IBaseTextureOptions<IVideoResourceOptions>, LoadVideoConfig>;
